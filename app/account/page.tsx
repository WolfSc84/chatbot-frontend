import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export default function AccountPage() {
  return (
    <PlaceholderPage
      title="Account"
      description="Account profiles, renewals, claims history and service status."
    />
  );
}
